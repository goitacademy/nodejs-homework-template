const contactValidateMiddleware = require('./contactValidateMiddleware');
const validateMiddleware = require('./validateMiddleware');
const authtenticate = require('./authtentificate');

module.exports = {
  contactValidateMiddleware,
  validateMiddleware,
  authtenticate,
};
