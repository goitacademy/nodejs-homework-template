const path = require("path");

const TEMP_DIR = path.join(__dirname, "../temp");
const PUBLIC_DIR = path.join(__dirname, "../public");
const AVATARS = path.join("avatars");
module.exports = { TEMP_DIR, PUBLIC_DIR, AVATARS };
