const path = require('path');
const TEMP_DIR = path.join(__dirname, '../temp');
const PUBLIC_DIR = path.join(__dirname, '../public');
const AVATARS = 'avatars';

module.exports = {
    TEMP_DIR, AVATARS, PUBLIC_DIR,
}