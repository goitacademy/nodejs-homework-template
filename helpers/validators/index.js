const contactValidator = require('./contact-validator');
const signupValidator = require('./user-validator');
const verify = require('./verify');

module.exports = {
    contactValidator,
    signupValidator,
    verify
};
