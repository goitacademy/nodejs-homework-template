const validateBody = require("./validateBody");
const isValidId = require("./isValidId");
const authenficate = require("./aunthenficate");
module.exports = {
  validateBody,
  isValidId,
  authenficate,
};
