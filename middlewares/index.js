const validation = require("./validation");
const controlslWrapper = require("./controlslWrapper");
const auth = require("./auth");

module.exports = {
    validation,
    controlslWrapper,
    auth
}