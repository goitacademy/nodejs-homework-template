const {
  validateBody,
  validateUpdateBody,
  validateUpdateBodyFavorite,
} = require("./validateBody");
const isValidId = require("./isValidId");

module.exports = {
  validateBody,
  validateUpdateBody,
  isValidId,
  validateUpdateBodyFavorite,
};
