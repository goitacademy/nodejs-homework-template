const { isEmptyBody } = require("./isEmptyBody");
const { isValidId } = require("./isValidId");
const { isEmptyFav } = require("./isEmptyFav");

module.exports = { isEmptyBody, isValidId, isEmptyFav };
