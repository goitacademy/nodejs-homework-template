const validation = require("./validation.js");
const ctrlWrapper = require("./ctrlWrapper.js");
const authWrapper = require("./auth.js");

module.exports = {
  validation,
  ctrlWrapper,
  authWrapper,
};
