const controller = require("./controllerWrapper");
const validation = require("./validation");
const authorization = require("./authorization");

module.exports = { controller, validation, authorization };
