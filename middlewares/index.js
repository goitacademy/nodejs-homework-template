const validateBody = require('./validateBody');
const authenticate = require('./authenticate');
const validateId = require('./validateObjId');

module.exports = { validateBody, authenticate, validateId };
