const validateBody = require("./validateBody");
const isValid = require("./isValid");
const authenticate = require("./authetincate");
const validateFavorite = require("./validateFavourite");
const validateSub = require("./validateSub");
module.exports = {
  validateBody,
  isValid,
  authenticate,
  validateFavorite,
  validateSub,
};
