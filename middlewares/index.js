const validateBody = require("./validateBody");
const isValidId = require("./isValidiId");
console.log(isValidId);
const authenticate = require("./autenticate");

module.exports = { validateBody, isValidId, authenticate };
