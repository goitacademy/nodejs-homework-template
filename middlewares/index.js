const validateBy = require("./validate");
const isValidId = require("./isValidId");
const authenticate = require("./authenticate");
const upload = require("./upload");
module.exports = {
  validateBy,
  isValidId,
  authenticate,
  upload,
};
