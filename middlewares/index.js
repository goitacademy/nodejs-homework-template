const { validation, validationFavorite } = require("./validation");
const isVaidId = require("./isValidId");
const autentificate = require("./autentificate");
module.exports = { validation, validationFavorite, isVaidId, autentificate };
