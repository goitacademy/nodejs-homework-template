const validation = require("./validation");
const controlWrapper = require("./controlWrapper");
const auth = require("./auth");
const upload = require("./upload");
const sizeChangerImage = require("./sizeChangerImage");
module.exports = {
  validation,
  controlWrapper,
  auth,
  upload,
  sizeChangerImage,
};
