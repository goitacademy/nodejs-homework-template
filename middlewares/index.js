const validateBody = require("./validateBody")
module.exports = {
    validateBody
const isValidId = require('./isValidId')
const auth = require('./auth')

module.exports = {
    validateBody,
    isValidId,
    auth,
const isValidId = require('./isValidId')

module.exports = {
    validateBody,
    isValidId
}