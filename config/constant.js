const fiftinMinutes = 15 * 60 * 1000;
const sixtyMinutes = 60 * 60 * 1000;

const maxLimiter = 1000;
const maxCreateAccountLimiter = 1000;

module.exports = {
  fiftinMinutes,
  sixtyMinutes,
  maxCreateAccountLimiter,
  maxLimiter,
};
