// const jsonwebtoken = require("jsonwebtoken");

// const payload = {
//   id: "dada78wd",
// };

// const foo = async () => {
//     const token = await jsonwebtoken.sign(payload, "qwerty", { expiresIn: "7h" });

//   const data = await jsonwebtoken.decode(token);
// };

// foo();
