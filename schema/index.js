const { schemaBody, schemaBodyObject, schemaStatusContact } = require('./schemaBody');
const { schemaRegister, schemaLogin, schemaEmail } = require('./schemaUser');

module.exports = {
  schemaBody,
  schemaBodyObject,
  schemaStatusContact,
  schemaRegister,
  schemaLogin,
  schemaEmail,
};
