const { schemaBody, schemaBodyObject, schemaStatusContact } = require('./schemaBody');
const { schemaRegister, schemaLogin } = require('./schemaUser');
module.exports = {
  schemaBody,
  schemaBodyObject,
  schemaStatusContact,
  schemaRegister,
  schemaLogin,
};
