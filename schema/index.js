const {
  addContactSchema,
  editContactSchema,
  editFavoriteContactSchema,
} = require("./validationSchema");

module.exports = {
  addContactSchema,
  editContactSchema,
  editFavoriteContactSchema,
};
