const contactSchema = require("./contactsSchema");
const contactsFavSchema = require("./contactsFavSchema");
const usersSchema = require("./usersSchema");

module.exports = { contactSchema, contactsFavSchema, usersSchema };
