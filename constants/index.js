const { phoneRegexp} = require("../constants/contacts");
const {emailRegexp, listSubscription} = require("../constants/users");

module.exports = {
  phoneRegexp,
  listSubscription,
  emailRegexp,
};
