const { Contact, bodyValidator, favoriteValidator } = require("./contact");
const {User, userValidator} = require('./user')

module.exports = {
  Contact,
  bodyValidator,
  favoriteValidator,
  User,
  userValidator,
};
