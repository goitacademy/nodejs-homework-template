const { Contact, joiSchema, favoriteJoiSchema } = require('./contacts');
const { User, schemas } = require('./user');

module.exports = {
  Contact,
  joiSchema,
  favoriteJoiSchema,
  User,
  schemas,
};