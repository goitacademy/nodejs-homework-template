const { Contact, schemas: contactSchemas } = require("./contact");
const { User, schemas: userSchemas } = require("./user");

module.exports = {
  Contact,
  User,
  contactSchemas,
  userSchemas,
};
