
const { Contact } = require('./contact');
const { User } = require('./user');

module.exports = {
    Contact,
    User

const { Contact } = require("./contact");
const { User } = require("./user");

module.exports = {
  Contact,
  User,

};
