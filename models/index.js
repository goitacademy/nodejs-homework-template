const { Contact, contactSchemas } = require("./contacts");
const { User, singupSchema, loginSchema } = require("./users");
module.exports = {
  Contact,
  User,
  contactSchemas,
  singupSchema,
  loginSchema,
};
