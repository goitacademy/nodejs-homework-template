/* eslint-disable eol-last */
const { Contact } = require('./contact')
const { User } = require('./user')
const { UserContact } = require('./user-contact')

module.exports = { Contact, User, UserContact }