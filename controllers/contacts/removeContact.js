const { Contact } = require('../../models/contact');

const removeContact = async (req, res) => {
//   const result = await Contact.remove(req.body);
//   res.status(201).json(result);
};

module.exports = removeContact;
