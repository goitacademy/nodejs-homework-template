const { getContacts, getById, addCont, deleteContact, changeContact } = require('../contacts/contactsController')

module.exports = {
  getContacts,
  getById,
  addCont,
  deleteContact,
  changeContact
}
