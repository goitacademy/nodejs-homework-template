const { getAll, getById, add, updateById, deleteById, updateStatusContact } = require("./contacts");


module.exports = {
    getAll,
    getById,
    add,
    updateById,
    deleteById,
    updateStatusContact,
}