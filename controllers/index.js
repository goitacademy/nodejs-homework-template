const contacts = require("./contacts");
const auth = require("./auth");
const users = require("./users");
const userContacts = require("./user-contacts");

module.exports = {
    contacts,
    auth,
    userContacts,
    users,
};