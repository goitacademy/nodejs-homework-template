const contactsControllers = require('./contacts');
const userAuthControllers = require('./auth');
const usersControllers = require('./users');

module.exports = { contactsControllers, userAuthControllers, usersControllers };