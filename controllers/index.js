const { getAll, getById, add, deleteById, updateById, updateStatusContact } = require('./contacts');

module.exports = {
  getAll,
  getById,
  add,
  deleteById,
  updateById,
  updateStatusContact,
};
