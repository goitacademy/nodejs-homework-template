const { listContacts, getContactById, addContact, removeContact, updateContact } = require("./controllersContacts")

module.exports = {
    listContacts,
    getContactById,
    addContact,
    removeContact,
    updateContact,
}