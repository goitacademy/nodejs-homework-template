const {
  signup,
  signin,
  getCurrent,
  signout,
  updateAvatar,
  verify,
} = require("./auth-controller");

module.exports = {
  signup,
  signin,
  getCurrent,
  signout,
  updateAvatar,
  verify,
};
