const {
  signup,
  signin,
  getCurrent,
  signout,
  updateAvatar,
  verifyEmail,
  resendEmail,
} = require("./auth-controller");

module.exports = {
  signup,
  signin,
  getCurrent,
  signout,
  updateAvatar,
  resendEmail,
  verifyEmail,
};
