const signup = require('./signup')
const login = require('./login')
const logout = require('./logout')
const current = require('./current')
const subscription = require('./subscription')
const updImg = require('./updImg')

module.exports = {
  signup,
  login,
  logout,
  current,
  subscription,
  updImg
}
