const { HttpCode } = require("./httpCode");
const { MESSAGES } = require("./messages");
const { MIN_AGE, MAX_AGE } = require("./contacts-const");

module.exports = { MIN_AGE, MAX_AGE, HttpCode, MESSAGES };
