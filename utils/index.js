const {
  addContactSchema,
  updateContactSchema,
  updateContactStatusSchema,
  validateId,
} = require('./validation');

module.exports = {
  addContactSchema,
  updateContactSchema,
  updateContactStatusSchema,
  validateId,
};
