const contactsRouter = require('./api/contacts');
const userAuthRouter = require('./api/auth');
const usersRouter = require('./api/users');

module.exports = { contactsRouter, userAuthRouter, usersRouter };