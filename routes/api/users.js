const express = require("express");
const router = express.Router();
// const controler = require("../../controlers/index");

router.post("/signup", async () => console.log("test"));

module.exports = router;
