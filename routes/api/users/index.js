const express = require("express");
const router = express.Router();
const contactsController = require("../../../controllers/user-controller");
const validate = require("./validation");

module.exports = router;
