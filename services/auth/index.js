const signup = require('./signup');
const login = require('./login');
const logout = require('./logout');
const patch = require('./subscription');
const sendEmail = require('./sendEmail');

module.exports = {
    signup,
    login,
    logout,
    patch,
    sendEmail,
};