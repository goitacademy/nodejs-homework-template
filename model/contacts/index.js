const { Contact } = require('./Contacts');
const { joiSchema } = require('./Contacts');
const { joiSchemaStatus } = require('./Contacts');

module.exports = {
  Contact,
  joiSchema,
  joiSchemaStatus
}
