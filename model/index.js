// Возвращает контакт с указанным contactId или null если контакт не найдет

// удаляет контакт с указанныим contactId если контакт удален возвращает true или false если такого контакта нет
const removeContactById = async contactId => {
  console.log('removeContact');
};

// добавляем контакт body, возвращаем добавленный контакт
const addContact = async body => {};

const updateContact = async (contactId, body) => {};

module.exports = {
  removeContactById,
  addContact,
  updateContact,
};
